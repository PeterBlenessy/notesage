# The third-party notice list is generated by `pnpm licenses:generate` from
# the npm and cargo manifests. It is committed so the app works offline and so
# CI's frontend job does not need a Rust toolchain — but it is machine output,
# so keep it out of diffs and out of the language stats.
src/generated/*.json -diff linguist-generated
