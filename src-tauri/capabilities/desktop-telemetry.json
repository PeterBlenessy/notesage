{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-telemetry",
  "description": "Usage-telemetry invoke bridge. Split out of `default` and platform-scoped because `tauri-plugin-aptabase` does not compile for iOS (its sys.rs leaves ENGINE_NAME undefined), so the dependency is gated off there — and a capability referencing a permission from a plugin that isn't built fails the build outright. The mobile app ships no usage telemetry by design.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["aptabase:allow-track-event"]
}
