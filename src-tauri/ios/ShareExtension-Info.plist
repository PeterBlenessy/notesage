<?xml version="1.0" encoding="UTF-8"?>
<!--
  Share Extension Info.plist (reference). Activates "Share → Notesage" when the
  shared item contains a web URL (Safari, X/Twitter, etc.) or plain text. Set
  NSExtensionPrincipalClass to the module-qualified ShareViewController, and set
  the target's bundle id under the app (e.g. com.notesage.app.ShareExtension).
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDisplayName</key>
    <string>Notesage</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.share-services</string>
        <key>NSExtensionPrincipalClass</key>
        <string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
        <key>NSExtensionAttributes</key>
        <dict>
            <!--
              Value is the JS file's basename WITHOUT the .js extension. Safari
              runs it in the PAGE's context before the sheet appears and hands
              the result to the extension, so we receive the DOM as rendered
              rather than as a server would serve it to a fetch. See
              share-preprocess.js for why that difference matters.
            -->
            <key>NSExtensionJavaScriptPreprocessingFile</key>
            <string>share-preprocess</string>
            <key>NSExtensionActivationRule</key>
            <dict>
                <!--
                  WebPage is what delivers the preprocessing payload; WebURL
                  stays because sharing from Messages, Mail or an in-app
                  browser hands over a bare URL with no page behind it, and
                  those must still activate the extension.
                -->
                <key>NSExtensionActivationSupportsWebPageWithMaxCount</key>
                <integer>1</integer>
                <key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
                <integer>1</integer>
                <key>NSExtensionActivationSupportsText</key>
                <true/>
                <key>NSExtensionActivationSupportsImageWithMaxCount</key>
                <integer>10</integer>
                <key>NSExtensionActivationSupportsMovieWithMaxCount</key>
                <integer>3</integer>
                <key>NSExtensionActivationSupportsFileWithMaxCount</key>
                <integer>3</integer>
            </dict>
        </dict>
    </dict>
</dict>
</plist>
