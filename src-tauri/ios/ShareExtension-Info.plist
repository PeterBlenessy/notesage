<?xml version="1.0" encoding="UTF-8"?>
<!--
  The Share Extension's Info.plist — the REAL one, for both platforms.

  iOS: `integrate-share-extension.py` copies this to
  `gen/apple/NotesageShare/Info.plist` and points INFOPLIST_FILE at that copy.
  macOS: `scripts/build-macos-share-extension.sh` copies it into the appex.

  It is copied rather than referenced so that release-time version stamping
  writes to a gitignored file — `ios-testflight.sh` promises never to rewrite a
  tracked file mid-release.

  This file used to be documentation only, mirroring a Python dict that was the
  thing actually built. An edit here changed nothing, which is a trap: the file
  a reviewer opens must be the file that ships. The dict is gone; this is it.

  The two version keys are absent on purpose. The copy step writes them from
  `tauri.ios.conf.json`, the same source Tauri reads for the app, so the pair
  can never drift — App Store Connect rejects an extension whose version
  disagrees with its host app.

  The $(...) values are build settings Xcode substitutes at build time; they are
  what xcodegen synthesised before this file took over, reproduced exactly.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>Notesage</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <!-- "XPC!" is the package type for an app extension; a bundle typed as
         anything else fails to load at RUNTIME, not at build. -->
    <key>CFBundlePackageType</key>
    <string>XPC!</string>
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.share-services</string>
        <key>NSExtensionPrincipalClass</key>
        <string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
        <key>NSExtensionAttributes</key>
        <dict>
            <!--
              Basename without ".js". Safari runs it in the PAGE's context
              before the sheet appears and hands us the result, so we receive
              the DOM as RENDERED rather than as a server serves it to a fetch
              — the difference between a real photo and a 40px lazy-load
              placeholder. See share-preprocess.js.
            -->
            <key>NSExtensionJavaScriptPreprocessingFile</key>
            <string>share-preprocess</string>
            <!--
              A predicate, not the dictionary shorthand. The shorthand's
              `File` key matches only attachments backed by a file URL — the
              Files app, QuickLook — and never a document handed over as
              in-memory data, which is how Safari's PDF viewer shares the
              page it is showing (`com.adobe.pdf` + `public.url`, no
              `public.file-url`). Measured in the simulator with a host that
              presents every item shape: file-URL PDF listed Notesage, PDF
              data did not (#843). No dictionary key can express "a PDF
              attachment", so the rule is spelled out.

              Match on ANY attachment conforming to a type the extension
              handles. `com.apple.property-list` is Safari's preprocessing
              payload (share-preprocess.js); `public.url` covers file URLs
              too. `public.plain-text` rather than the shorthand's broader
              `public.text`: the extension reads only plain text, so being
              listed for rich text (RTF) meant "nothing to save" — narrower
              on purpose. `public.data` is deliberately absent: a provider
              that offers only bare data has no name and no type to save it as.
            -->
            <key>NSExtensionActivationRule</key>
            <string>SUBQUERY(extensionItems, $extensionItem, SUBQUERY($extensionItem.attachments, $attachment, ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "com.apple.property-list" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.url" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.plain-text" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "com.adobe.pdf" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "org.idpf.epub-container" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.image" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.movie" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.audio").@count &gt;= 1).@count &gt;= 1</string>
        </dict>
    </dict>
</dict>
</plist>
