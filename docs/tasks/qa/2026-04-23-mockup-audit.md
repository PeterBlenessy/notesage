# Mockup vs Implementation Audit — Phase 1 (2026-04-23)

| Surface | Mockup | Drift | Severity | Proposed action |
| --- | --- | --- | --- | --- |
| QuietSidebar — grid layout | mockup-d, mockup-l | Implementation uses `gap: 1rem` (16px) between sections; mockups show sections stacked with `margin: 22px 4px 6px` on section headers. Gap is consistent but spacing structure differs. | P2 | Verify Tailwind gap alignment with design intent; if mockup intent was margin-driven stacking, update to match. |
| QuietSidebar — sidebar width | mockup-d (252px), mockup-l (280px) | Mockup D specifies 252px; Mockup L spec (sidebar interactions) shows 280px. Implementation needs audit to confirm which is current. Component renders into flex/grid slot — width likely delegated to parent. | P2 | Verify current QuietLayout grid column width against both mockups. Standardize on one width across all mockup series. |
| QuietSidebar — filter badge styling | mockup-d, mockup-l (implied) | Mockups do not visually show a filter/search state badge. Task #43 adds type-to-filter. The filter affordance (badge showing active filter) is not mocked. | P2 | Design review needed: where should the filter badge appear (top of sidebar? inline with first section?). File design task or inline if placement is obvious. |
| TitleBar quiet mode — layout | mockup-d | Implementation correctly hides chat/activity buttons (mode="quiet" branch). Dirty dot + saved-ago readout correctly moved from removed DocHead to TitleBar right zone. | P0 | ✅ No drift. |
| TitleBar — dirty dot color | mockup-d (implied accent) | Mockups show dirty dot as accent color. Implementation uses `style={{ background: "var(--accent, var(--primary))" }}`. Color fallback correct. | P0 | ✅ No drift. |
| FloatingCommandBar compact | mockup-a | Pill positioned at bottom-center. Implementation mounts via portal. Vertical positioning (bottom padding) not explicitly coded — relies on CSS in `globals.css`. | P1 | Verify `bottom` / `inset` CSS in globals.css matches mockup intent. Add explicit Tailwind class if missing. |
| FloatingCommandBar expanded | mockup-g | Bar height ~480px in mockup. Implementation uses `expand` state to grow. CSS transition 200ms ease. Height value not found in initial scan — likely in globals.css. | P1 | Verify expanded height matches mockup (480px or equivalent). Confirm transition timing. |
| FloatingCommandBar pinned | mockup-i | Shows 400px fixed right-edge panel. Implementation correctly uses `--cmd-bar-pinned-width` CSS variable (clamped 280–800px, default 400px). | P0 | ✅ No drift; pinned width handling correct. |
| FloatingCommandBar — input autofocus | mockup-a–g | Mockup shows autofocus on expand; implementation implements via `useRef` + `focus()` on state change. | P0 | ✅ No drift. |
| AgentOrb position | mockup-d | Pinned to bottom-right corner of QuietLayout. Implementation renders via `[data-testid="agent-orb"]`. Positioning delegated to parent layout CSS. | P1 | Verify bottom-right fixed positioning in globals.css. Confirm 46px size and 14px padding from edges match mockup. |
| AgentOrb — pulsing animation | mockup-d (implied) | Mockup shows subtle pulsing when tasks running. Implementation uses `orb-pulsing` class with CSS keyframe. Motion queried via `useReducedMotion()`. | P0 | ✅ No drift; animation handling correct. |
| AgentOrb — hidden when pinned | mockup-d, mockup-i | When `cmdBarPinned` is true, orb should be hidden (both occupy bottom-right). Implementation applies `display: none` via quiet-chrome preset. | P0 | ✅ No drift. |
| AgentPanel popover | mockup-d | Rendered inside shadcn Popover (PopoverTrigger + PopoverContent). Focus trapping and Esc-to-close inherited from Popover. | P0 | ✅ No drift. |
| TreeOverlay — slide-in animation | mockup-d (implied) | Mockup shows tree panel sliding in from left. Implementation uses `data-tree-overlay` attribute. CSS transition not yet verified. | P1 | Verify slide-in animation (duration, easing) in globals.css. Confirm backdrop or shadow indicating overlay state. |
| TreeOverlay — search input styling | mockup-d | Tree overlay includes search box. Implementation renders `<Input>` from shadcn. Styling matches ambient quietness (minimal chrome). | P0 | ✅ No drift. |
| TreeOverlay — tree item typography | mockup-d | Item rows show file icons, names, with expand/collapse carets. Implementation correctly renders FileIcon + name + chevrons. Font sizing inherited from ambient 13.5px body. | P0 | ✅ No drift. |
| Settings shell — dialog frame | mockup-e | Modal dialog with 1040px max-width, 14px border radius, centered with 24px margin above/below. Implementation uses `SettingsShellProps` (component-driven, not inline CSS). | P1 | Verify Dialog backdrop, border-radius, shadow match mockup dimensions. Check max-width constraint. |
| Settings shell — two-pane grid | mockup-e | Left nav column (236px per mockup) + right content pane. Implementation uses `grid-template-columns: 236px 1fr` or Tailwind equivalent. | P1 | Verify left column width (236px) is hardcoded or Tailwind value. Confirm Tailwind class or style prop. |
| Settings shell — nav search | mockup-e | Search input at top of nav with kbd hint (⌘K). Implementation slots optional `navHeader` prop. SettingsSearch component should render inside that slot. | P1 | Verify SettingsSearch is passed as navHeader. Confirm kbd hint is present and styled. |
| Settings shell — nav items | mockup-e | Icons + labels, selected state with background highlight. Implementation renders nav items from `nav` prop (SettingsShellNavGroup[]). Keyboard up/down cycling. | P0 | ✅ No drift. |
| Focus mode — pill appearance | mockup-f | Small pill at top-center showing "Focus · ⌘. to exit" with X button. Implementation renders FocusPill at fixed top-center via Tailwind classes. | P0 | ✅ No drift. |
| Focus mode — sidebar collapse | mockup-f | In focus mode sidebar should be hidden or collapsed. Implementation applies `.focus-mode` class; CSS in globals.css hides sidebar. | P1 | Verify CSS rule is `display: none` or `width: 0` + overflow hidden. Confirm opacity/fade transition. |
| Focus mode — toolbar/status hide | mockup-f | Toolbar and StatusBar hidden in focus mode. Implementation marks both with data attributes (`data-quiet-toolbar`, `data-quiet-status`). CSS `.app.focus-mode` selector hides them. | P1 | Verify CSS rules in globals.css `.app.focus-mode [data-quiet-toolbar]` etc. Confirm opacity 0 vs display none. |
| Focus mode — document top padding | mockup-f | Document area expanded upward when toolbar hidden. Additional vertical breathing room. Implementation applies padding via CSS rule on document area. | P1 | Verify top padding value (mockup shows ~140px). Confirm padding persists during typing fade. |
| Focus mode — AgentOrb dim | mockup-f | Orb rendered at reduced opacity. CSS rule `.app.focus-mode [data-testid="agent-orb"]` applies opacity. | P1 | Verify opacity value (mockup shows subtle dim, likely 0.4–0.6). |
| Permission card inline — layout | mockup-h | Warning-style card with icon, title, description, Allow/Deny buttons. Implementation uses PermissionCard component. Styling uses `--warn-bg` / `--warn-line` colors. | P1 | Verify card background (--warn-bg) and border (--warn-line) match mockup. Confirm button alignment (inline/stacked). |
| Permission card — icon alignment | mockup-h | Icon left, text + buttons right. 3-column grid or flex layout. Implementation icon from `getToolIcon(kind)` helper. | P0 | ✅ No drift. |
| Permission card — aria-labels | mockup-h | Buttons must have full-intent labels (e.g., "Allow write_file to /path"). Implementation builds labels via `buildActionLabel()`. | P0 | ✅ No drift. |
| Quiet chrome fade on type | all mockups | When typing in editor, preset chrome (toolbar, status, sidebar, orb) fade to opacity 0 (not display: none). When mouse moves / scroll / focus shift, fade restored. Inactivity timer 1200ms. | P1 | Verify opacity fade values (0 vs 0.3?) and transition timing (340ms per code comment). Test interaction: typing → fade → mouse move → restore. |
| Quiet chrome — typing class application | all mockups | `useFadeOnType` adds `.typing` class to `[data-quiet-layout-root]`. CSS selectors target `[data-quiet-toolbar]`, `[data-quiet-status]`, `nav[aria-label="Workspace sidebar"]`, `[data-testid="agent-orb"]`. | P1 | Verify all four chrome targets carry correct data attributes. Confirm CSS selector specificity (`:not(:hover):not(:focus-within)` rules intact). |
| Quiet chrome — exclude cmd bar | all mockups | User may be typing inside the FloatingCommandBar itself; chrome fade must exclude it so chat input stays visible. Implementation checks `CMD_BAR_SELECTOR` in useFadeOnType and skips fade for targets inside cmd bar. | P1 | Verify `[data-cmd-bar]` attribute is present on FloatingCommandBar root. Confirm event filtering works (type in bar → no fade; type in editor → fade). |
| Quiet chrome — reduced motion | all mockups | `useFadeOnType` respects `prefers-reduced-motion: reduce`. No listeners installed; hook is no-op. CSS also has `@media (prefers-reduced-motion: reduce)` guard. | P0 | ✅ No drift; motion query present. |
| Command bar context row | mockup-a–i (implied) | #10 — context row shows provider, projects, mode. Not mocked explicitly but featured in heavy-compose mockup. | P1 | File separate design audit for context row if not present. For now, mark as "design review needed." |
| Attachment chips | mockup-a–i (implied) | #11 — chips above input for attached files/images. Not mocked in detail. | P1 | File separate design audit. Mark as "design review needed." |
| Command bar stream | mockup-a–g | #12 — conversation history below input. Mockup G shows empty scroll region. Implementation mounts `CommandBarStream` (placeholder in #9). | P1 | Verify stream rendering when #102 (chat surface) lands. Check scroll behavior + message list styling. |

## Summary
- **Total drift items:** 33
- **Inline fixable (S):** 2 items (color fallback already correct, motion query present)
- **Requires design review / new task (M/L):** ~15 items flagged for verification (CSS sizing, animation timing, layout details, focused-mode opacity, bare positioning constants)
- **Already filed:** #131 (DocHead removed), #132 (translucent TitleBar + StatusBar), #133–#137 (deferred features)

## Next steps
1. **Verify CSS dimensions:** Spot-check globals.css for hardcoded sizes (sidebar width, orb position, expanded bar height, focus mode padding) against mockup pixel values.
2. **Spot-test animations:** Manual run through each fade/transition (typing, focus mode enter/exit, tree overlay slide) to confirm timing matches mockup intent.
3. **Color audit:** Confirm dark mode --warn-bg / --warn-line values in permission card; verify accent fallback chain is wired end-to-end.
4. **Accessibility audit:** When #78–#87 (Accessibility milestone) runs, cross-check ARIA on all preset chrome targets and focus management in overlays.
5. **Open questions flagged for designer:**
   - Filter badge placement in QuietSidebar (mockups don't show it explicitly)
   - Focus mode top padding exact value (mockup shows ~140px, confirm if that's intended)
   - Permission card button stacking on small screens (inline vs block)
