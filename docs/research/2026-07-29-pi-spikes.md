# pi Local Agent Preset — M0 Spike Findings

**Date:** 2026-07-29 (macOS #2 run 2026-07-30) **Status:** Complete — all three spikes PASS; go

| Stage | Link | Status |
| --- | --- | --- |
| PRD | [pi-local-agent-preset](../prds/2026-07-29-pi-local-agent-preset.md) | Implemented |
| Tasks | [pi-local-agent-preset-tasks](../tasks/2026-07-29-pi-local-agent-preset-tasks.md) | In Progress |

De-risking spikes for adopting pi (`earendil-works/pi`) as a second Local Agent preset. **Pinned version under test: pi v0.80.6** (linux-x64 Bun-compiled release binary). All transcripts reproduced from a live harness run; nothing below is inferred from docs alone.

## Spike #1 — Bun-binary parity (RPC + extension loading) — ✅ PASS

Executed on linux-x64 (this exercises the same `bun build --compile` pipeline as the darwin assets; a darwin corroboration run rides the spike-#2 CI workflow).

**Verified:**

- **Checksum verification works as designed.** `SHA256SUMS` is published per release with one line per asset; `sha256sum -c` verified `pi-linux-x64.tar.gz` cleanly. This is the asset `agent_manager.rs` task #15 will verify against.
- **Folder-tarball layout confirmed.** The archive extracts to a `pi/` directory (~122 MB): the `pi` executable plus co-located `photon_rs_bg.wasm`, `theme/`, `assets/`, `node_modules/`, `examples/`, `package.json`. The executable must stay inside this folder — install = extract whole dir, symlink/exec `pi/pi`.
- **The Bun-compiled binary runs without any system runtime.** `--version` → `0.80.6`; no Node/Bun install present was used.
- **`--mode rpc` JSONL framing works.** `{"type":"prompt","message":…}` on stdin produced the full event lifecycle on stdout: `response` → `agent_start` → `turn_start` → `message_start/update(text_delta)/end` → `turn_end` → `agent_end` → `agent_settled`. Exactly the surface the bridge (#7/#8) maps to ACP.
- **TypeScript extensions load in the compiled binary.** A marker extension in `$PI_CODING_AGENT_DIR/extensions/` (type-only import of `ExtensionAPI`, runtime `node:fs`) executed at startup and its `pi.on("tool_call", …)` hook fired during turns.
- **Custom provider + dummy key work.** `models.json` with `{ baseUrl: "http://127.0.0.1:<port>/v1", api: "openai-completions", apiKey: "dummy" }` streamed from a stub OpenAI-compatible server; the stub received `Authorization: Bearer dummy`.

**Findings that correct prior assumptions:**

1. **`PI_CODING_AGENT_DIR` layout is FLAT.** pi writes `auth.json`, reads `models.json`, `extensions/` directly under the dir — there is no `agent/` nesting when redirected (the `agent/` segment belongs to the default `~/.pi/agent` path only). Task #16 must write `models.json`/`settings.json`/`extensions/` at the top of `~/.notesage/agents/pi/`.
2. **pi probes `GET /v1/models` on the configured baseUrl** at session start even with models enumerated in `models.json` (sent without auth header). Harmless for the preset — same localhost origin as the llama-server, which serves `/v1/models` — but the stub in any harness must answer it.
3. Bundled `node_modules` still carries the `@mariozechner` scope alongside `@earendil-works` — cosmetic, but version-pin bumps should re-check extension import specifiers.

## Spike #2 — Zero-network under Seatbelt (PI_OFFLINE=1) — ✅ PASS (macOS, pi v0.80.6 darwin-arm64, 2026-07-30)

Harness: `scripts/spikes/pi-seatbelt-spike.sh` (fetch + `SHA256SUMS` verify → flat pi-home → deny-all-network Seatbelt profile with a single `localhost:<port>` allow → three scenarios via `scripts/spikes/pi-seatbelt/rpc-turn.mjs`), also dispatchable as `.github/workflows/spike-pi-seatbelt.yml` on `macos-14`. Run on the operator's Mac; verbatim result:

| Scenario (all under the deny-all-network Seatbelt profile) | Result |
| --- | --- |
| **baseline** (no proxy env) | ✅ turn settled in 1538 ms, `text="Hello from stub."` — a full agentic turn completes with only `localhost:8137` reachable at the kernel level |
| **proxy-env** (`HTTP(S)_PROXY`→unreachable, no `NO_PROXY`) | ❌ **exit 3** — turn settled after 15 545 ms with empty text: **pi honors `HTTP(S)_PROXY` for the localhost call** and routed the model request into the (unreachable) proxy. This is the *desired* demonstration that the mitigation is needed. |
| **no-proxy** (`HTTP(S)_PROXY` set + `NO_PROXY=localhost,127.0.0.1`) | ✅ turn settled in 1540 ms, `text="Hello from stub."` — the mitigation restores direct localhost access |

Extension marker loaded under the sandbox (`loaded pid=49908`) — **TS extensions load inside the Seatbelt-confined Bun binary** too. Stub saw only its two localhost POSTs (`auth=Bearer dummy`); no non-localhost traffic. `SPIKE #2: PASS`.

**Correction to the earlier Linux finding.** On linux-x64 the proxy-env scenario had *passed* (pi appeared to ignore proxy vars), so the doc briefly called `NO_PROXY` defense-in-depth. **The macOS run falsifies that**: on the target platform pi's undici stack DOES honor `HTTP(S)_PROXY`, so `NO_PROXY=localhost,127.0.0.1` is a **required** part of the task-#16 config, not optional. The shipped config already sets it unconditionally (`build_pi_env`), and the code comments + test were corrected to say so. Without it, in production the model call would route into Notesage's per-agent domain-filtering proxy (which doesn't allow the llama port) and stall — exactly the 15 s hang seen here.

**Why this is safe.** pi's only network egress is the llama-server on localhost (PI_OFFLINE=1 + deny-all kernel sandbox block everything else). `NO_PROXY=localhost` makes pi hit the kernel-allowed llama port directly — identical to how Goose talks to the same server — while every non-localhost destination stays blocked at the kernel.

## Spike #3 — extension_ui_request permission round-trip — ✅ PASS (with one load-bearing caveat)

Harness: stub server returns a `write` tool call (OpenAI streaming `tool_calls` deltas) on the first completion; a gate extension (`pi.on("tool_call")` → `ctx.ui.select("Allow write?", ["Yes","No"])`) guards it; an RPC driver plays four scenarios.

| Scenario | Result |
| --- | --- |
| **allow** (`extension_ui_response {value:"Yes"}`) | ✅ `tool_execution_start` → UI request → response → tool executes, file written, turn completes |
| **deny** (`{value:"No"}`) | ✅ gate returns `{block:true,reason:"denied by user"}` → `tool_execution_end` with `isError:true` and the reason as the tool result; model continues; file NOT written |
| **abort with the UI request unanswered** | ⚠️ **WEDGES.** After `{"type":"abort"}` pi emitted nothing further — no abort ack, no `agent_end` — for 20+ s until SIGKILL. The blocked `ctx.ui.select` promise is not cancelled by abort. File not written (fail-safe held). |
| **cancel** (`{cancelled:true}`) | ✅ `ctx.ui.select` resolves `undefined` → gate blocks → turn completes normally |
| **cancel-then-abort** | ✅ pending request answered first, then `abort` acks (`response command:abort success:true`) |

**Facts confirmed:** `ctx.hasUI === true` in RPC mode (the gate's interactive path is live, with block-by-default when `hasUI` is false, e.g. `-p` mode). The `extension_ui_request` carries `{id, method:"select", title, options}` — everything the bridge needs to synthesize an ACP `request_permission`.

**Load-bearing rule for bridge task #9:** never lead with `abort` while an `extension_ui_request` is outstanding. The deny/timeout path is: send `extension_ui_response {cancelled:true}` (or `value:"No"`), *then* abort if the turn itself must die. The 30s auto-deny maps to `cancelled:true`.

## Go / no-go

**GO (task #4, 2026-07-30).** All three spikes pass on the pinned pi v0.80.6:

- **#1** (Bun-binary parity) — PASS (linux-x64; darwin corroborated by #2's marker-under-Seatbelt).
- **#2** (zero-network under Seatbelt) — PASS on macOS: a full agentic turn completes under the deny-all kernel profile with only the llama port reachable, and TS extensions load inside the confined binary. The one required config consequence — `NO_PROXY` to keep pi's proxy-honoring undici stack off Notesage's domain proxy — is shipped in task #16 and regression-tested.
- **#3** (permission round-trip) — PASS, with the answer-before-abort rule baked into the bridge (#9).

No confinement or protocol blocker remains. The initiative proceeds; the only outstanding gate is **#22**, the in-app live end-to-end verification (interactive, operator-run).

## Harness artifacts

Developed in the session scratchpad; the durable versions land with task #2 as `scripts/spikes/pi-seatbelt-spike.sh` + `.github/workflows/spike-pi-seatbelt.yml` (stub server, marker/gate extensions, RPC drivers for `allow|deny|abort|cancel|cancel-then-abort`).
