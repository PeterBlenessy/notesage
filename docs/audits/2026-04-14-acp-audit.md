# ACP Implementation Audit — Notesage

**Date:** 2026-04-14 **Status:** Audit complete, implementation done

| Stage | Link | Status |
| --- | --- | --- |
| PRD | [acp-protocol-compliance](../prds/2026-04-14-acp-protocol-compliance.md) | Implemented |
| Tasks | [acp-protocol-compliance-tasks](../tasks/2026-04-14-acp-protocol-compliance-tasks.md) | All 27 tasks complete ✅ |
| PRD | [acp-rich-tool-content](../prds/2026-04-17-acp-rich-tool-content.md) | Implemented |
| Tasks | [acp-rich-tool-content-tasks](../tasks/2026-04-17-acp-rich-tool-content-tasks.md) | All 8 tasks complete ✅ |
| PRD | [acp-session-lifecycle-completeness](../prds/2026-04-17-acp-session-lifecycle-completeness.md) | Implemented |
| Tasks | [acp-session-lifecycle-completeness-tasks](../tasks/2026-04-17-acp-session-lifecycle-completeness-tasks.md) | All 13 tasks complete ✅ |
| PRD | [acp-terminal-client-capability](../prds/2026-04-18-acp-terminal-client-capability.md) | Draft |
| Tasks | — | Not planned |
| PRD | [acp-protocol-tail](../prds/2026-04-18-acp-protocol-tail.md) | Implemented (v0.37.0) |
| Tasks | [acp-protocol-tail-tasks](../tasks/2026-04-18-acp-protocol-tail-tasks.md) | All 13 tasks complete ✅ |

**ACP Schema Crate:** v0.11.7 (latest)\
**Notesage ACP Crate:** v0.10.4 (runtime, latest) / v0.11.4 (schema, pinned by runtime) — runtime crate pins `=0.11.4`, can't bump schema independently\
**Notesage Unstable Features:** `unstable_session_model`, `unstable_session_usage` (v0.34.0); `unstable_session_close`, `unstable_session_fork`, `unstable_session_resume` (v0.36.0)

---

## Executive Summary

Notesage has a **functional, production-quality ACP integration** that covers the core happy path: spawn agent → authenticate → create session → stream prompts with tool calls and permissions. The implementation includes strong process management (SIGKILL cleanup, PID tracking, reconnect) and a custom `JsonLineFilter` to handle misbehaving agents.

*As of audit date (2026-04-14):* the implementation covers roughly **40% of the ACP surface area**. Major gaps include: no thinking/reasoning segment handling in chat, no session modes, no config options, no plan display, no slash command passthrough, no file system/terminal client capabilities, no usage tracking in UI, and no support for any of the newer unstable features (elicitation, NES, session fork/resume/close, session list). Several provider-specific workarounds are hardcoded where ACP provides dynamic alternatives.

**Biggest wins by effort-to-impact ratio:**

1. **Thinking segments in chat** — low effort, high UX impact (already handled in task operations)
2. **Crate version bump** — low effort, unlocks new capabilities
3. **Session modes** — medium effort, enables code/architect/ask switching
4. **Usage tracking display** — low effort, already receiving data
5. **Client FS/terminal capabilities** — medium effort, unlocks faster agent operations

> **Post-implementation update (v0.34.0):** All Phase 1–4 tasks from the PRD have been completed. ACP feature coverage raised from 41% to \~70%. Thinking in chat, session modes, dynamic config options, usage tracking, plan display, slash command passthrough, capability probing, eager session creation, session restoration, and mode-sandbox conflict resolution are all implemented.

> **Post-implementation update (v0.36.0):** Two additional batches shipped. **Batch B** — inline tool call content: file diffs render as collapsible unified diffs and text content blocks as collapsible monospace output (chat + delegation activity panel); terminal blocks show a placeholder. **Batch C** — session lifecycle completeness: `session/resume`, `session/load`, `session/list`, `session/new` chained capability-gated for restoration; `session/fork` powers branch isolation; `session/close` fires on conversation delete. Four related fixes uncovered during integration: camelCase capability reads, `hasHydrated()` gate on the eager effect, in-flight lock to dedupe React strict-mode firings, conversation-switch re-triggers the restoration chain. Coverage now \~71%. Remaining stable-spec gaps: `user_message_chunk`, `audio`/`resource_link`/`resource` content blocks, terminal client capabilities (blocked on `terminal/create`). Explicit won't-fix: client FS capabilities.

---

## Protocol Feature Matrix

### Legend

- ✅ Implemented and working
- ⚠️ Partially implemented or has issues
- ❌ Not implemented
- 🔒 Unstable feature (behind Cargo feature flag)
- N/A Not applicable

### Core Protocol

| ACP Feature | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| JSON-RPC 2.0 transport | ✅ | v0.33.0 | Via `agent-client-protocol` crate (bumped to latest in v0.34.0) |
| Stdio transport | ✅ | v0.33.0 | Agent subprocess stdin/stdout |
| `initialize` handshake | ✅ | v0.33.0 | v0.34.0 also extracts `capabilities` (AgentCapabilities) for capability probing |
| Protocol version negotiation | ✅ | v0.33.0 | Sends `ProtocolVersion::V1` |
| `_meta` extension fields | ⚠️ | x | Not read or used, but not blocked |
| Extension methods (`_` prefix) | ❌ | x | Not supported — no dispatch for `_`-prefixed methods |

### Authentication

| ACP Feature | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| `authenticate` request | ✅ | v0.33.0 | Calls with method_id or first available |
| `Agent` auth type | ✅ | v0.33.0 | Agent opens browser for OAuth |
| `EnvVar` auth type 🔒 | ⚠️ | v0.37.0 | `unstable_auth_methods` feature enabled; `AuthMethod::EnvVar` serialized to the frontend with `vars[]` + `link`; generic EnvVar credential form replaces the Gemini-specific panel. `credentials.envVars` keychain storage preserved for backwards compatibility |
| `Terminal` auth type 🔒 | ❌ | ❌ | Not implemented. Gemini terminal auth uses `run_in_terminal` Tauri command instead |
| `logout` 🔒 | ❌ | ❌ | Not implemented |
| Auth status pre-check | ⚠️ | Removed in v0.37.0 | Hardcoded per-provider CLI checks (`claude auth status`, `codex auth status`, etc.) deleted in v0.37.0 — auth state now flows from the ACP `authenticate` response, with a stored-artifact fast path to avoid spawning agents purely to discover they need auth |

### Session Lifecycle

| ACP Feature | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| `session/new` | ✅ | v0.33.0 | v0.34.0 added `modes` + `config_options` extraction and eager creation on chat open |
| `session/load` | ✅ | v0.33.0 | Used in reconnect + session restoration. Gated on `loadSession` capability (v0.34.0) |
| `session/list` 🔒 | ❌ | v0.36.0 | Sanity check in the restoration chain when resume+load both fail |
| `session/prompt` | ✅ | v0.33.0 | Full implementation with images |
| `session/set_mode` | ❌ | v0.34.0 | Mode picker UI, permission-level mapping, capability probing, connection defaults |
| `session/set_config_option` | ❌ | v0.34.0 | Dynamic config options, thinking effort migration from hardcoded Codex field |
| `session/fork` 🔒 | ❌ | v0.36.0 | Leaf-branching calls `session/fork` when the agent advertises the capability; historical branches share the parent session |
| `session/resume` 🔒 | ❌ | v0.36.0 | Preferred over `session/load` in the restoration chain (lightweight takeover) |
| `session/close` 🔒 | ❌ | v0.36.0 | Best-effort close fires on conversation delete so agents can free resources |
| `session/set_model` 🔒 | ⚠️ | v0.34.0 | Post-session `set_model` replaces CLI arg injection |
| `cancel` notification | ✅ | v0.33.0 | v0.34.0 added proper `Cancelled` responses to pending permissions per spec |
| MCP server passthrough | ❌ | ❌ | Sessions created without `mcpServers` — Notesage has its own MCP client. Deferred (see Batch C). |
| Additional directories 🔒 | ❌ | ❌ | Not implemented |

### Streaming (Session Updates)

| ACP Update Type | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| `agent_message_chunk` (text) | ✅ | v0.33.0 | Appended to message + text segment |
| `agent_message_chunk` (image) | ✅ | v0.33.0 | Pushed as image segment |
| `agent_thought_chunk` | ⚠️ | v0.34.0 | Handled in both chat and task listeners as ThinkingSegment |
| `tool_call` | ✅ | v0.33.0 | Full segment + activity tracking with FIFO queue |
| `tool_call_update` | ⚠️ | v0.34.0 | Handles status, locations, rawOutput — not just label |
| `tool_result` | ⚠️ | ❌ | Custom event — not a standard ACP update type. Notesage infers tool completion from permission flow patterns |
| `agent_turn_complete` | ✅ | v0.33.0 | Finalizes segments and activities |
| `plan` | ❌ | v0.34.0 | PlanSegment with collapsible entries, status/priority indicators |
| `available_commands_update` | ❌ | v0.34.0 | Agent slash commands in chat input `/` menu |
| `current_mode_update` | ❌ | v0.34.0 | Mode changes reflected in mode picker UI |
| `config_option_update` | ❌ | v0.34.0 | Config option changes update dynamic dropdowns |
| `session_info_update` | ❌ | v0.34.0 | Agent-generated titles used in conversation history |
| `usage_update` 🔒 | ❌ | v0.34.0 | Token count in chat footer with cost tooltip |
| `user_message_chunk` | ❌ | v0.37.0 | Recognized as a silent no-op in both chat and task listeners; suppresses the `Unknown session update type` debug log. We already have the user message locally — the echo is discarded |

### Content Blocks

| Content Type | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| `text` | ✅ | v0.33.0 | Full support |
| `image` | ✅ | v0.33.0 | Sending and receiving images with base64 data |
| `audio` | ❌ | ❌ | Not supported |
| `resource_link` | ❌ | v0.37.0 | Normalized in `acp-utils.ts` and appended as a markdown link to the current text segment. `file://` URIs inside a project open as editor tabs; other URIs open in the system browser |
| `resource` (embedded) | ❌ | ❌ | Not supported |

### Tool Calls

| ACP Feature | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| Tool call display | ✅ | v0.33.0 | Segments with kind, label, detail, status |
| Tool call update tracking | ⚠️ | v0.34.0 | Status, locations, rawOutput tracked |
| `ToolKind` mapping | ✅ | v0.33.0 | `formatAcpToolName()` maps all standard kinds |
| `ToolCallContent::content` | ⚠️ | v0.36.0 | Text content rendered as collapsible monospace block below the tool label (chat + activity panel) |
| `ToolCallContent::diff` | ❌ | v0.36.0 | File diffs rendered inline as a collapsible unified diff with `+/-` coloring via `--color-diff-*` CSS variables (chat + activity panel) |
| `ToolCallContent::terminal` | ❌ | v0.36.0 (placeholder) | Rendered as a muted placeholder — no terminal ownership yet. Full support requires `terminal/create` client capability |
| `ToolCallLocation` (follow-along) | ❌ | v0.34.0 | Locations stored on segments, clickable file links |
| rawInput display | ✅ | v0.33.0 | Shown in segment detail + hover |
| rawOutput display | ❌ | v0.34.0 | Shown in tool result segments |
| Parallel tool calls | ✅ | v0.33.0 | FIFO queue handles multiple concurrent tool calls |

### Permission Model

| ACP Feature | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| `request_permission` handling | ✅ | v0.33.0 | Full implementation with channel-based async wait |
| `PermissionOptionKind` respect | ⚠️ | ❌ | Options displayed, but Notesage adds its own tier system (session/always) that may not align with agent-provided option semantics |
| Cancel → pending permission cleanup | ⚠️ | v0.34.0 | Properly responds `Cancelled` to all pending permissions per spec |
| Permission auto-approval | ✅ | v0.33.0 | Read tools auto-approved, write tools require confirmation |
| Path filtering | ✅ | v0.33.0 | Denies tool calls outside project scope for comment-sourced chats |

### Client Capabilities

| Capability | Advertised | Audit Impl | Shipped in | Notes |
| --- | --- | --- | --- | --- |
| `fs.readTextFile` | ❌ | ❌ | won't fix | Agent reads files directly. See SKIPPED decision in "Next Batch Planning". |
| `fs.writeTextFile` | ❌ | ❌ | won't fix | Agent writes files directly. Filesystem watcher covers editor follow-along. |
| `terminal` | ❌ | ❌ | ❌ | Agent manages its own terminal/command execution (candidate for a future batch) |
| `clientInfo` | ✅ | ✅ | v0.33.0 | Sends name="Notesage" + version |

### Agent Capabilities (Read)

| Capability | Audit Read | Audit Used | Shipped in | Notes |
| --- | --- | --- | --- | --- |
| `agentInfo` | ✅ | ✅ | v0.33.0 | Stored in SpawnResult |
| `authMethods` | ✅ | ✅ | v0.33.0 | Passed to frontend for auth flow |
| `loadSession` | ❌ | — | v0.34.0 | Checked before `session/load`, used in session restoration |
| `promptCapabilities.image` | ✅ | ✅ | v0.33.0 | Controls image attachment UI |
| `promptCapabilities.audio` | ❌ | — | ❌ | Not checked |
| `promptCapabilities.embeddedContext` | ❌ | — | ❌ | Not checked |
| `mcpCapabilities` | ❌ | — | ❌ | Not checked |
| `sessionCapabilities` | ❌ | — | v0.34.0 | Capability checking before gated methods (list/fork/resume/close wired in v0.36.0) |
| `modes` (from session/new) | ❌ | — | v0.34.0 | Extracted, stored, used for mode picker + capability probing |
| `configOptions` (from session/new) | ❌ | — | v0.34.0 | Extracted, stored, rendered as dynamic dropdowns |

### Process Management

| Feature | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| `kill_on_drop(true)` | ✅ | v0.33.0 | Set on subprocess |
| SIGKILL via `libc::kill()` | ✅ | v0.33.0 | For forced cleanup |
| PID tracking (`Arc<AtomicU32>`) | ✅ | v0.33.0 | Thread-safe PID sharing |
| `RunEvent::Exit` cleanup | ✅ | v0.33.0 | `stop_all_sync()` drains all agents |
| Thread join with timeout | ✅ | v0.33.0 | 500ms timeout per thread |
| Frontend `beforeunload` | ✅ | v0.33.0 | Secondary cleanup defense |
| Agent exit event | ✅ | v0.33.0 | `acp-agent-exited` with exit code |
| Reconnect with session restore | ✅ | v0.33.0 | Kill old → spawn new → authenticate → load session |
| Liveness check | ✅ | v0.33.0 | `acp_is_agent_alive` checks thread handle |

### Network Sandboxing

| Feature | Audit Status | Shipped in | Notes |
| --- | --- | --- | --- |
| HTTP proxy injection | ✅ | v0.33.0 | `HTTP_PROXY`, `HTTPS_PROXY`, `NO_PROXY` env vars |
| Kernel-level Seatbelt | ✅ | v0.33.0 | `(deny default)` with proxy-only allow |
| Domain allowlists | ✅ | v0.33.0 | Per-provider defaults + user-configurable |
| Domain approval UI | ✅ | v0.33.0 | Allow once/session/always/deny |
| Violation monitoring | ✅ | v0.33.0 | macOS log stream → Activity panel |
| Proxy cleanup on exit | ✅ | v0.33.0 | Spawns temp runtime for async cleanup |

---

## Detailed Gap Analysis

### 1. Thinking/Reasoning in Chat — ~~HIGH PRIORITY~~ ✅ RESOLVED

**Gap:** `agent_thought_chunk` events are handled in `useAgentTaskOperations.ts` (background agent tasks) but **completely ignored** in `useAcpSessionListeners.ts` (interactive chat). Users see no thinking/reasoning output from Claude Code, Codex, or other agents in the chat panel.

**Resolution:** Added `agent_thought_chunk` handling to `setupAcpChatListeners()`. ThinkingSegments rendered in chat with collapsible UI. Also added dual-write consistency in task operations.

### 2. Crate Version Drift — ~~HIGH PRIORITY~~ ✅ RESOLVED

**Gap:** Using `agent-client-protocol` v0.10.4 and schema v0.11.4 while latest is v0.11.6. The crate is actively evolving.

**Resolution:** Both crates bumped to latest. Unknown `SessionUpdate` variants now handled gracefully with debug logging.

### 3. Session Modes — ~~MEDIUM PRIORITY~~ ✅ RESOLVED

**Gap:** No support for `session/set_mode`. Claude Code supports "code", "architect", and "ask" modes. Other agents may support similar modes. The `modes` field from `session/new` response is not extracted.

**Resolution:** Full implementation: mode picker UI with permission-level mapping (Read Only / Agent / Full Access / Plan), capability probing at connection registration, connection defaults, eager session creation, mode-sandbox conflict dialog.

### 4. Usage Tracking — ~~LOW PRIORITY, FREE WIN~~ ✅ RESOLVED

**Gap:** `unstable_session_usage` feature is enabled in Cargo.toml, but `usage_update` events are completely ignored in the frontend. The data is being received and discarded.

**Resolution:** `usage_update` events parsed and displayed as token count in chat footer with cost tooltip on hover.

### 5. Client FS/Terminal Capabilities — **MEDIUM PRIORITY** (still open)

**Gap:** Notesage advertises no client capabilities (`fs.readTextFile`, `fs.writeTextFile`, `terminal`). This means agents must handle all file I/O and command execution themselves.

**Impact:**

- Agents can't delegate file reads to the client (which already has the files open)
- Agents can't use the client's terminal for command execution
- Agent operations are slower (agent reads files from disk instead of client serving from memory)
- No follow-along editor integration (agent edits files but editor doesn't update)

**Fix:**

1. Implement `Client::read_text_file()` — serve from editor state or filesystem
2. Implement `Client::write_text_file()` — write + update editor
3. Implement `Client::create_terminal()` etc. — delegate to Tauri shell
4. Advertise capabilities in `InitializeRequest`

**Effort:** High (significant new functionality), but high long-term value

### 6. Tool Call Update Handling — ~~MEDIUM PRIORITY~~ ✅ RESOLVED

**Gap:** `tool_call_update` events only update the active tool label. The spec says updates can change: `title`, `kind`, `status`, `content` (array replacement), `locations`, `rawInput`, `rawOutput`.

**Resolution:** Expanded handler to track status transitions, store locations as clickable file links, and display rawOutput in tool result segments (v0.34.0). Diff and text content rendering shipped in v0.36.0 (Batch B); terminal content is a placeholder pending a `terminal/create` client capability.

### 7. Agent Plan Display — ~~LOW PRIORITY~~ ✅ RESOLVED

**Gap:** `plan` session updates are ignored. Agents like Claude Code send execution plans for complex tasks.

**Resolution:** PlanSegment type added. Plans render as collapsible cards with status icons (pending/in_progress/completed) and priority dots.

### 8. Session Info Updates — ~~LOW PRIORITY, FREE WIN~~ ✅ RESOLVED

**Gap:** `session_info_update` events (containing session `title`) are ignored. Agents generate titles for conversations.

**Resolution:** `session_info_update` handled — agent-generated titles automatically applied to chat history.

### 9. Slash Command Passthrough — ~~LOW PRIORITY~~ ✅ RESOLVED

**Gap:** `available_commands_update` events are ignored. Agents advertise slash commands (e.g., Claude Code's `/compact`, `/clear`, `/bug`, etc.).

**Resolution:** Agent commands stored from `available_commands_update` and surfaced in the `/` command menu alongside Notesage skills, distinguished by Terminal icon.

### 10. Config Options — ~~LOW PRIORITY~~ ✅ RESOLVED

**Gap:** `session/set_config_option` not implemented. Agents provide config options (e.g., thinking effort) via the protocol, but Notesage uses hardcoded UI for these settings.

**Resolution:** Dynamic config options extracted from session response, rendered as dropdowns in chat footer. Mode and model categories filtered (handled by dedicated pickers). Hardcoded Codex `reasoningEffort` migrated to dynamic `acpDefaults.thinkingEffort`.

---

## Hardcoded Values That Could Be Dynamic

| What | Where | Audit State | Shipped in | Should Be |
| --- | --- | --- | --- | --- |
| Model flag format | `acp-agent-state.ts` | `--model` for most, `-c model=` for codex | v0.34.0 (replaced with `session/set_model`) | Done |
| Reasoning effort | `acp-agent-state.ts` | Codex-specific `model/effort` suffix | v0.34.0 (replaced with `session/set_config_option`) | Done |
| Auth status check | `acp_binary.rs` | Per-provider CLI commands | v0.37.0 | Deleted — ACP `authenticate` flow handles it; stored-artifact fast path avoids unnecessary spawns |
| Gemini ACP confirm | `acp.rs` | Pre-sends "Y\\n" to stdin | ❌ | Should be handled by Gemini's `--yes` or `-y` flag |
| Tool name mapping | `acp-utils.ts` | Hardcoded kind→label map | ❌ | Could use `ToolCall.title` as primary label |
| Connection error patterns | `errors.ts` | Hardcoded string patterns | ❌ | Could use error codes from JSON-RPC |
| Provider domain defaults | `connections-store.ts` | Hardcoded per-provider allowlists | ❌ | Could be derived from agent binary name or `agentInfo` |
| Init timeout | `acp.rs` | 30s hardcoded | ❌ | Could be configurable per connection |
| Prompt timeout | `acp.rs` | 30 minutes hardcoded | ❌ | Could be configurable or use heartbeat |
| Unresponsive timeout | `useAcpLifecycle.ts` | 5 minutes hardcoded | ❌ | Could adapt based on agent type |

---

## Custom Extensions & Workarounds

| Extension | Location | Purpose | Risk |
| --- | --- | --- | --- |
| `JsonLineFilter` | `acp_client.rs:120-268` | Strips non-JSON lines from agent stdout | **Necessary** — Gemini CLI writes prompts to stdout. Well-implemented with long-line passthrough. |
| Gemini "Y\\n" pre-send | `acp.rs:352` | Confirms ACP mode | **Fragile** — depends on Gemini CLI interactive prompt behavior |
| `tool_result` pseudo-event | `useAcpSessionListeners.ts:130` | Inferred tool completion | **Non-standard** — ACP uses `tool_call_update` with `status: completed`. Notesage's logic relies on permission flow patterns |
| System prompt prepend | `useAcpLifecycle.ts:404-432` | Injects Notesage context into first message | **Workaround** — ACP doesn't have a system prompt field; could use MCP context or resource blocks |
| Singleton agent state | `acp-agent-state.ts` | Module-level mutable singleton | **Architecture risk** — not reactive, requires manual sync with React state |
| SIGKILL direct | `acp.rs:107,1040` | Forced process cleanup | **Aggressive** — doesn't give agent a chance to clean up; could corrupt agent state files |
| Env var auth for Gemini | `acp-agent-state.ts` | Passes API key as env var | **Non-standard** — should use ACP `EnvVar` auth method when available |

---

## Robustness Assessment

### Strengths

1. **Process lifecycle management** is thorough: `kill_on_drop`, PID tracking, `RunEvent::Exit` hook, frontend `beforeunload`, SIGKILL fallback
2. **Reconnect flow** is well-designed: kill old → spawn new → authenticate → load session → resend prompt
3. **Permission handling** is correct: channel-based async wait, proper cancellation on agent stop
4. **JsonLineFilter** is production-quality: handles edge cases (long lines, partial buffers, EOF)
5. **Error recovery** includes one-time auto-retry for connection errors
6. **Network sandboxing** is defense-in-depth: kernel Seatbelt + proxy + domain allowlists

### Weaknesses

1. **~~No capability checking~~** ~~— calls~~ `session/load` ~~without checking~~ `agentCapabilities.loadSession`~~, calls~~ `session/set_model` ~~(implicitly via CLI args) without checking support~~ **Resolved in v0.34.0/v0.36.0:** capability probing at connection registration; `hasLoadSessionCapability` / `hasSessionCapability` helpers tolerant of both camelCase (spec) and snake_case payloads; gated calls for load / list / fork / resume / close / set_model.
2. **~~Deserialization brittleness~~** ~~— unknown~~ `SessionUpdate` ~~variants may cause parse errors rather than graceful unknown handling~~ **Resolved in v0.34.0:** unknown `SessionUpdate` variants log at `debug` and continue processing.
3. **Single-threaded tokio** — `ClientSideConnection` is `!Send`, forcing a `LocalSet` on a dedicated OS thread. Thread panics silently kill the agent connection
4. **No reconnect on partial failure** — if authenticate succeeds but session/load fails, the agent is in an authenticated but sessionless state
5. **Silent event drops** — `let _ = self.app.emit(...)` in `session_notification()` silently drops events if the window is gone
6. **30-minute hard timeout** — may be insufficient for very long research tasks, but also means a hung prompt blocks the thread for 30 minutes

---

## Updateability Assessment

| Dimension | Audit Rating | Shipped in | Notes |
| --- | --- | --- | --- |
| Crate updates | ⚠️ | v0.34.0 | Bumped to latest; graceful unknown variant handling |
| New update types | ❌ | v0.34.0 | Unknown `SessionUpdate` variants logged gracefully instead of crashing |
| New auth methods | ⚠️ | ❌ | Only `Agent` type truly supported; new types need code changes |
| New content blocks | ⚠️ | ❌ | Only `text` and `image` handled; `audio`, `resource_link`, `resource` ignored |
| New tool content types | ❌ | v0.36.0 | Text + rawOutput (v0.34.0), diff (v0.36.0). Terminal placeholder only — full support blocked on `terminal/create` |
| Provider additions | ⚠️ | v0.34.0 | Model flags via `set_model`, config via `set_config_option` — less per-provider hardcoding |
| Feature flag adoption | ⚠️ | v0.36.0 | v0.34.0 wired `usage_update`; v0.36.0 added `session_close`/`session_fork`/`session_resume`. 5 of 13 unstable features enabled. |

---

## Extensibility Opportunities

### Near-term (protocol-compliant)

1. **Expose agent modes** in chat footer → users can switch code/architect/ask
2. **Show usage data** already flowing → token counts, costs, context window usage
3. **Use session titles** from agents → better conversation history naming
4. **Dynamic config options** → auto-discovered settings UI
5. **MCP server passthrough** → let agents connect to user's MCP servers

### Medium-term (new capabilities)

1. **Client FS capabilities** → faster reads, editor follow-along
2. **Client terminal** → integrated command execution with output streaming
3. **Plan visualization** → show agent's execution plan before it starts
4. **Agent slash commands** → `/compact`, `/clear`, `/bug`, `/init` in chat input
5. **Session list/resume** → browse and resume prior agent sessions

### Long-term (architectural)

1. **NES (Next Edit Suggestions)** — ACP's equivalent of inline code completion, richer than single-token suggest. RPCs: `nes/start`, `nes/suggest`, `nes/accept`, `nes/reject`, `nes/close`, plus document-lifecycle notifications (`didChange`/`didOpen`/`didClose`/`didFocus`/`didSave`). Essentially a miniature LSP tunneled through ACP.
   - *Why it matters:* any ACP agent could drive ghost-text inline completions, not just the Copilot LSP binary. Unlocks Claude/Codex-powered inline suggestions without a separate LSP.
   - *Why deferred:* large surface area (1–2 weeks). Needs document-lifecycle plumbing from Tiptap/ProseMirror, integration with the existing `GhostText` extension, and coordination with the Copilot LSP + local FIM paths so they don't fight. Warrants a deliberate "how should inline completion work?" redesign, not a tail cleanup.
2. **Elicitation** — agent requests structured input from the user mid-task via a JSON-Schema-described form instead of a free-text question. Example: "I need a deployment target" → agent sends a form with region/env/cluster dropdowns → user fills in → agent resumes with typed response.
   - *Why it matters:* today's agents handle structured input through natural-language parsing, which is unreliable for deploy configs, API credentials, filter selection, etc. Elicitation gives type-safe validated input.
   - *Why deferred:* no agent in the wild (Claude Code / Codex / Copilot CLI / Gemini) currently emits elicitation requests. Waiting for demand is cheaper than speculating a UI. Implementation is a JSON-Schema form renderer (e.g. `@rjsf/core`) + modal surface + two RPCs.
3. **Resource blocks** — agents can reference and embed documents (`resource` content block). Unlike `resource_link` which is just a URI, `resource` can embed the full content inline in the message.
4. **Multi-root workspaces** — `additionalDirectories` for monorepo support, so a single session can operate across multiple project roots without respawning the agent.
5. *(Folded into the [acp-protocol-tail](../prds/2026-04-18-acp-protocol-tail.md) PRD)* `messageId` **tracking** — stable per-message IDs (via `unstable_message_id`) that survive session save/load. Propagates through `PromptRequest.message_id` (outbound) and `agent_message_chunk.message_id` + `user_message_id` (inbound). Forward-compatibility groundwork for NES referencing past messages, plan linkage, richer session/load round-trips.
6. *(Shipped in v0.36.0)* **~~Session forking~~** ~~→ parallel exploration of different approaches~~

---

## Recommended Way Forward

### Phase 1 — Quick Wins ✅ COMPLETE

| Task | Impact | Effort | Status |
| --- | --- | --- | --- |
| Add `agent_thought_chunk` to chat listeners | High — thinking visible | 20 lines | ✅ |
| Bump ACP crates to latest | High — compatibility | 1 hour | ✅ |
| Handle `session_info_update` for conversation titles | Medium — better history | 10 lines | ✅ |
| Parse and display `usage_update` in chat | Medium — cost visibility | 2-3 hours | ✅ |
| Gracefully handle unknown `SessionUpdate` variants | High — robustness | 30 min | ✅ |

### Phase 2 — Core Gaps ✅ COMPLETE

| Task | Impact | Effort | Status |
| --- | --- | --- | --- |
| Session modes (extract, display, switch) | High — major UX feature | 1-2 days | ✅ |
| Dynamic config options (extract, render, apply) | Medium — replaces hardcoded | 1-2 days | ✅ |
| Rich tool call updates (status, locations, rawOutput) | Medium — better follow-along | 1 day | ✅ |
| Replace hardcoded model flags with `session/set_model` | Medium — cleaner architecture | 0.5 days | ✅ |

### Phase 2B — Capability Probing & Session Lifecycle ✅ COMPLETE

| Task | Impact | Effort | Status |
| --- | --- | --- | --- |
| Capability probe at connection registration | High — immediate mode picker | Medium | ✅ |
| Connection config defaults for mode and thinking effort | Medium — user preferences | Medium | ✅ |
| Eager session creation at chat open | High — instant controls | Large | ✅ |
| Session restoration for existing chats | Medium — context preservation | Medium | ✅ |
| Mode-sandbox conflict resolution dialog | Medium — security UX | Large | ✅ |
| Migrate hardcoded thinking effort to dynamic config | Medium — clean architecture | Small | ✅ |

### Phase 3 — Rich Streaming Data ✅ COMPLETE

| Task | Impact | Effort | Status |
| --- | --- | --- | --- |
| Agent plan display | Medium — transparency | 1-2 days | ✅ |
| Agent slash command passthrough | Medium — power user feature | 2-3 days | ✅ |

### Remaining Work

| Task | Impact | Effort | Status |
| --- | --- | --- | --- |
| Client FS capabilities (`readTextFile`, `writeTextFile`) | High — performance + follow-along | 3-5 days | won't fix (see SKIPPED decision below) |
| Client terminal capabilities | High — command integration | 1 week | not started |
| MCP server passthrough to sessions | Medium — tool ecosystem | 1-2 days | not started |
| Session list/fork/resume/close | Medium — session persistence | 3-5 days | ✅ shipped in v0.36.0 (Batch C) |
| NES integration | High — inline suggestions from ACP | 1-2 weeks | not started |
| Elicitation support | Low-Medium — structured input | 3-5 days | not started |
| Tool call content (diff) | Medium — richer display | 2-3 days | ✅ shipped in v0.36.0 (Batch B) |
| Tool call content (terminal) | Medium — richer display | 2-3 days | placeholder only; full support blocked on `terminal/create` |
| Task agent session lifecycle (Batch C-bis) | Medium — popover chat persistence | Medium | not started (filed 2026-04-17) |

---

## Provider Comparison Matrix

| Capability | Claude Code | Codex | Copilot CLI | Gemini CLI |
| --- | --- | --- | --- | --- |
| **Auth method** | Agent (browser OAuth) | Agent (browser OAuth) | Agent (GitHub device flow) | Agent (custom) + EnvVar |
| **Session modes** | `default`, `acceptEdits`, `plan`, `dontAsk`, `bypassPermissions` | `read-only`, `auto`, `full-access` (no Plan) | `#agent`, `#plan`, `#autopilot` (URL-based, no Read Only) | `default`, `autoEdit`, `yolo`, `plan` (plan buggy in ACP — [#22191](https://github.com/google-gemini/gemini-cli/issues/22191)) |
| **Config options** | Thinking effort (`thought_level`) | Thinking effort (`reasoning_effort` / `thought_level`) | Likely (reasoning effort at minimum) | ❌ None exposed ([#21974](https://github.com/google-gemini/gemini-cli/issues/21974)) |
| **Thinking content** | ✅ Extended thinking | ⚠️ Handled generically — may not emit `agent_thought_chunk` | ⚠️ Unknown — terminal-only feature, may not forward via ACP | ❌ Never emitted — Gemini API bug ([#20977](https://github.com/google-gemini/gemini-cli/issues/20977)) |
| **Tool calling** | Comprehensive | Comprehensive | Comprehensive | Comprehensive |
| **Session load** | ✅ | ✅ Session persistence supported | ⚠️ Unknown — persists sessions locally, ACP support unconfirmed | ❌ Always creates new sessions ([#15502](https://github.com/google-gemini/gemini-cli/issues/15502)) |
| **Usage updates** | ✅ | ✅ Token usage tracked | ⚠️ Unlikely — subscription model, no docs confirm it | ❌ Not emitted |
| **Plans** | ✅ Plan streaming updates | ❌ "Plan mode" is `read-only` session mode, not ACP plan events | ⚠️ Likely in Plan mode — generates multi-step plans | ❌ Writes plan to file, not ACP plan events; plan mode broken in ACP |
| **Slash commands** | ✅ /compact, /clear, etc. | ⚠️ Likely — has /status, /plan, /compact; depends on adapter version | ❌ Not advertised via ACP ([#1044](https://github.com/github/copilot-cli/issues/1044)) | ❌ Not advertised via ACP |
| **Image support** | ✅ `promptCapabilities.images` | ✅ Supports image attachments | ✅ Multimodal prompts confirmed | ✅ Multimodal (Gemini models) |
| **Stdout cleanliness** | Clean JSON-RPC | Clean JSON-RPC | Clean JSON-RPC | Requires JsonLineFilter |
| **ACP flag** | Native ACP binary | Native ACP binary | `--acp` flag | `--experimental-acp -y` |
| **Binary distribution** | npm global | GitHub releases | npm global | pip/brew |

---

## Appendix: All ACP Features Inventory

Complete list of every ACP protocol feature with Notesage implementation status:

| \# | Category | Feature | Stable? | Audit Status | Shipped in |
| --- | --- | --- | --- | --- | --- |
| 1 | Transport | JSON-RPC 2.0 | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 2 | Transport | Stdio | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 3 | Init | `initialize` request | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 4 | Init | Protocol version negotiation | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 5 | Init | Client capabilities advertisement | <span style="color: rgb(34, 197, 94);">✔</span> | ⚠️ No FS/terminal | <span style="color: rgb(34, 197, 94);">✔ </span>won't fix (FS), x (terminal) |
| 6 | Init | Agent capabilities extraction | <span style="color: rgb(34, 197, 94);">✔</span> | ⚠️ Only images | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 7 | Auth | `authenticate` request | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 8 | Auth | `Agent` auth type | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 9 | Auth | `EnvVar` auth type | 🔒 | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.37.0 |
| 10 | Auth | `Terminal` auth type | 🔒 | ❌ | ❌ |
| 11 | Auth | `logout` | 🔒 | ❌ | ❌ |
| 12 | Session | `session/new` | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 (v0.34.0 added eager creation) |
| 13 | Session | `session/load` | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 (v0.34.0 added capability gating) |
| 14 | Session | `session/list` | 🔒 | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.36.0 |
| 15 | Session | `session/prompt` | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 16 | Session | `session/set_mode` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 17 | Session | `session/set_config_option` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 18 | Session | `session/fork` | 🔒 | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.36.0 |
| 19 | Session | `session/resume` | 🔒 | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.36.0 |
| 20 | Session | `session/close` | 🔒 | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.36.0 |
| 21 | Session | `session/set_model` | 🔒 | ⚠️ Feature enabled, not used | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 22 | Session | `cancel` notification | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 (v0.34.0 added Cancelled compliance) |
| 23 | Session | MCP server passthrough | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 24 | Stream | `agent_message_chunk` (text) | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 25 | Stream | `agent_message_chunk` (image) | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 26 | Stream | `agent_thought_chunk` | <span style="color: rgb(34, 197, 94);">✔</span> | ⚠️ Tasks only | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 27 | Stream | `tool_call` | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 28 | Stream | `tool_call_update` | <span style="color: rgb(34, 197, 94);">✔</span> | ⚠️ Label only | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 29 | Stream | `plan` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 30 | Stream | `available_commands_update` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 31 | Stream | `current_mode_update` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 32 | Stream | `config_option_update` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 33 | Stream | `session_info_update` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 34 | Stream | `usage_update` | 🔒 | ❌ (enabled, not used) | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 35 | Stream | `user_message_chunk` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.37.0 |
| 36 | Content | `text` block | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 37 | Content | `image` block | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 38 | Content | `audio` block | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 39 | Content | `resource_link` block | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.37.0 |
| 40 | Content | `resource` (embedded) block | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 41 | Tool | Permission request/response | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔</span> | <span style="color: rgb(34, 197, 94);">✔ </span>v0.33.0 |
| 42 | Tool | Permission option kinds | <span style="color: rgb(34, 197, 94);">✔</span> | ⚠️ Custom tier overlay | x (custom tier overlay) |
| 43 | Tool | Tool call content (text) | <span style="color: rgb(34, 197, 94);">✔</span> | ⚠️ Partial | <span style="color: rgb(34, 197, 94);">✔ </span>v0.36.0 |
| 44 | Tool | Tool call content (diff) | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.36.0 |
| 45 | Tool | Tool call content (terminal) | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.36.0 (placeholder) |
| 46 | Tool | Tool call locations | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.34.0 |
| 47 | Client | `fs/read_text_file` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | won't fix |
| 48 | Client | `fs/write_text_file` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | won't fix |
| 49 | Client | `terminal/create` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 50 | Client | `terminal/output` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 51 | Client | `terminal/wait_for_exit` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 52 | Client | `terminal/kill` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 53 | Client | `terminal/release` | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 54 | Ext | Extension methods (`_` prefix) | <span style="color: rgb(34, 197, 94);">✔</span> | ❌ | ❌ |
| 55 | Ext | `_meta` on all types | <span style="color: rgb(34, 197, 94);">✔</span> | ⚠️ Passthrough | ❌ (passthrough only) |
| 56 | NES | Next Edit Suggestions | 🔒 | ❌ | ❌ |
| 57 | Elicit | `elicitation/create` | 🔒 | ❌ | ❌ |
| 58 | Elicit | `elicitation/complete` | 🔒 | ❌ | ❌ |
| 59 | Message | `messageId` tracking | 🔒 | ❌ | <span style="color: rgb(34, 197, 94);">✔ </span>v0.37.0 |

**Audit summary:** 24 of 59 features implemented (41%). Of the 35 stable features, 16 implemented (46%).

**Current summary:** 46 of 59 features implemented (78%). Of the 35 stable features, 30 implemented (86%). +22 features implemented since audit. Batch C-bis + D adds `EnvVar` auth (#9), `user_message_chunk` (#35), `resource_link` (#39), and `messageId` tracking (#59) — shipped in v0.37.0.

---

## Next Batch Planning (2026-04-17)

### Client FS Capabilities — SKIPPED

**Decision:** Skip `fs/read_text_file` (#47) and `fs/write_text_file` (#48) indefinitely. Not worth the complexity.

**Reasons:**

1. **Dual representation mismatch:** The editor holds a ProseMirror document tree, not raw markdown. Serving "file content from memory" requires serializing ProseMirror → markdown on every agent read request — slower than the agent reading from disk directly, and risks introducing serialization artifacts.
2. **IPC is slower than direct disk access:** Agent reads a file in microseconds. Routing through JSON-RPC → Notesage → serialize → JSON-RPC response is orders of magnitude slower.
3. **Watcher already covers follow-along:** The filesystem watcher (500ms debounce, content comparison, auto-reload) handles the "editor updates when agent edits files" case well enough. Adding a parallel write-through-IPC path creates two codepaths for the same thing.
4. **Uncertain agent adoption:** Whether Claude Code, Codex, Copilot, or Gemini actually delegate file I/O to the client (vs using their own tools) is unconfirmed. The capability advertisement may go unused.
5. **Dirty content serving is a footgun:** The only unique benefit — serving unsaved editor content — is a niche edge case that could confuse agents (they'd see content that doesn't exist on disk).

### Recommended Next Batches

**Batch B — Tool Call Content Richness (next):**

| Feature | ACP # | Effort |
| --- | --- | --- |
| Tool call content — `diff` display | #44 | Medium |
| Tool call content — `terminal` output | #45 | Medium |

Inline file diffs and terminal output in chat messages instead of opaque tool labels.

**Batch C — Session Lifecycle Completeness** (shipped 2026-04-17):

| Feature | ACP # | Effort | Status |
| --- | --- | --- | --- |
| `session/close` (graceful end) | #20 | Small | <span style="color: rgb(34, 197, 94);">✔</span> Shipped |
| `session/list` | #14 | Small-Medium | <span style="color: rgb(34, 197, 94);">✔</span> Shipped |
| `session/resume` | #19 | Medium | <span style="color: rgb(34, 197, 94);">✔</span> Shipped |
| `session/fork` | #18 | Medium | <span style="color: rgb(34, 197, 94);">✔</span> Shipped |
| MCP server passthrough | #23 | Medium | Deferred to a later batch |

**Batch C-bis + D — ACP Protocol Tail** (bundled in [acp-protocol-tail](../prds/2026-04-18-acp-protocol-tail.md) PRD) — <span style="color: rgb(34, 197, 94);">✔</span> Shipped in v0.37.0:

| Feature | ACP # | Effort | Group | Status |
| --- | --- | --- | --- | --- |
| Apply Batch C restoration chain to `useAgentTaskOperations` | — | Medium | Task agent parity | <span style="color: rgb(34, 197, 94);">✔</span> v0.37.0 |
| `session/close` on task completion | — | Small | Task agent parity | <span style="color: rgb(34, 197, 94);">✔</span> v0.37.0 |
| `user_message_chunk` handling | #35 | Small | Streaming/content | <span style="color: rgb(34, 197, 94);">✔</span> v0.37.0 |
| `resource_link` content blocks | #39 | Small | Streaming/content | <span style="color: rgb(34, 197, 94);">✔</span> v0.37.0 |
| `messageId` propagation | #59 | Small | Streaming/content | <span style="color: rgb(34, 197, 94);">✔</span> v0.37.0 |
| `EnvVar` auth type (proper ACP way) | #9 | Small | Auth | <span style="color: rgb(34, 197, 94);">✔</span> v0.37.0 |
| Replace hardcoded auth status checks | — | Medium | Auth | <span style="color: rgb(34, 197, 94);">✔</span> v0.37.0 |

**Batch E — Unstable Features (future):** see [Extensibility Opportunities → Long-term](#long-term-architectural) for what each is, why it matters, and why it's deferred.

| Feature | ACP # | Effort |
| --- | --- | --- |
| NES (Next Edit Suggestions) | #56 | Large (\~1–2 weeks) |
| Elicitation (`elicitation/create`/`complete`) | #57–58 | Medium |

**Batch F — Agent Terminal (client-hosted, sandboxed):**

| Feature | ACP # | Effort | Notes |
| --- | --- | --- | --- |
| `terminal/create` + `terminal/output` + `terminal/wait_for_exit` + `terminal/kill` + `terminal/release` | #49–53 | Large (\~1 week + security review) | Inverts command execution: agent asks client to run commands. Inherits the agent's Seatbelt profile so existing filesystem + network sandboxing applies unchanged. Per-command permission cards reuse the existing ACP tool-call permission tier. Live-streaming xterm.js widget replaces the current `ToolCallContent::Terminal` placeholder (#45). PRD: [acp-terminal-client-capability](../prds/2026-04-18-acp-terminal-client-capability.md). |

---

*Generated by Claude Code audit, 2026-04-14. Implementation status updated 2026-04-17.*